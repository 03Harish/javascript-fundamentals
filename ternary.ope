const age=17;
const result=age>=18?"eligible":"not eligible";
console.log(result);

//handling null value
function welcome(name){
    const result=name?name:"no name";
    console.log("welcome",result);
}
welcome();

//in object type variable
   user={'name': 'harish','age':25};
   console.log(user);//if we want only name
   console.log(user.name);

   //conditional chain
   const avg=90;
   const grade=avg>=90? "A grade":avg>=80? "B grade":"C grade";
   console.log("grade: ",grade);